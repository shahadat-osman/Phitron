#include <bits/stdc++.h>
using namespace std;
int main()
{
    // vector<int> a;
    // for (int i = 0; i < 10; i++)
    // {
    //     // cout<<"Before"<<a.size()<<"\n";
    //     a.push_back(i);
    // }

    // a.insert(a.begin() +0, 100);
    // a.erase(a.begin()+1);
    // for (int i = 0; i < a.size(); i++)
    // {
    //     cout << a[i] << "\n";
    // }
    vector<int>a;
    a.resize(10);
    return 0;
}