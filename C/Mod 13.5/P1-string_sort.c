#include<stdio.h>
#include<string.h>
int main()
{
    char sent[1000], m_a[27];
    scanf("%s", sent);
    int i, j, index, count=0;
    for(i=0; i<=26; i++)
    {
        m_a[i]=0;
    }
    for(i=0; i<strlen(sent); i++)
    {
        
    }

    
    /* for()
    {
    } */
    printf("%d", m_a[0]);
    return 0;
}