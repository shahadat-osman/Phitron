#include<stdio.h>
int main()
{
    int t, i, n;
    scanf("%d", &t);
    for(i=0; i<t; i++)
    {
        scanf("%d", &n);
        int ary[n];
        for(i=0; i<n; i++)
        {
            scanf("%d", &ary[i]);
        }
    }
}